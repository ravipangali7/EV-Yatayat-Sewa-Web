Add beep.mp3 here for:
- Driver "New seat(s) booked" popup sound (Vehicle page)
- Checkout/dropoff popup sound when running the web app standalone (outside Flutter WebView)

When in the Flutter app, you can use the same file from app/assets/sounds/beep.mp3 and play it from the native bundle if needed.
